
export class Coordinate{
  x:number;
  y:number;
  constructor(x:number, y:number) {
    this.x = x;
    this.y = y;
  }
  getY(){
    return this.y;
  }
  setY(y){
    this.y = y
  }
  getX(){
    return this.x;
  }
  setY(x){
    this.x = x
  }
}
